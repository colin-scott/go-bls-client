You will need to clone https://github.com/cridenour/go-bls and run `go install github.com/cridenour/go-bls`. I may have cut off the cridenour prefix, fwiw.

Then to run, `go build`, and `./bls -series <SERIES>`
